import React,{useState} from "react"; export default function Composer({onSend,disabled}){const [v,setV]=useState("");return <form onSubmit={e=>{e.preventDefault();if(v.trim()){onSend?.(v);setV("")}}} className="flex gap-2"><input value={v} onChange={e=>setV(e.target.value)} disabled={disabled} placeholder="Message..." className="min-w-0 flex-1 rounded-2xl border border-white/10 bg-white/5 px-4 py-3 text-white outline-none"/><button className="rounded-2xl bg-white px-5 text-black" disabled={disabled}>Send</button></form>}