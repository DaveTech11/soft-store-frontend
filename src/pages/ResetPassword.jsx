import React,{useState} from "react"; import {Link,useNavigate} from "react-router-dom"; import {api} from "@/api/client"; export default function ResetPassword(){const [password,setPassword]=useState(""),[done,setDone]=useState(false),nav=useNavigate();const submit=async e=>{e.preventDefault();const token=new URLSearchParams(location.search).get("token")||"";await api.auth.resetPassword({token,password});setDone(true);setTimeout(()=>nav("/login"),800)};return <main className="min-h-screen bg-[#08080a] p-8 text-white"><div className="mx-auto max-w-md py-20"><Link to="/login">← Back</Link><h1 className="mt-8 text-2xl font-bold">Choose a new password</h1>{done?<p className="mt-4 text-neutral-400">Password reset requested. Returning to sign in…</p>:<form onSubmit={submit} className="mt-6 space-y-3"><input required type="password" minLength="6" value={password} onChange={e=>setPassword(e.target.value)} placeholder="New password" className="w-full rounded-xl border border-white/10 bg-white/5 p-3 text-white"/><button className="w-full rounded-xl bg-white p-3 text-black">Reset password</button></form>}</div></main>}