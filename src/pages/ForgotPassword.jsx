import React,{useState} from "react"; import {Link} from "react-router-dom"; import {api} from "@/api/client"; export default function ForgotPassword(){const [email,setEmail]=useState(""),[done,setDone]=useState(false);return <main className="min-h-screen bg-[#08080a] p-8 text-white"><div className="mx-auto max-w-md py-20"><Link to="/login">← Back</Link><h1 className="mt-8 text-2xl font-bold">Reset password</h1>{done?<p className="mt-4 text-neutral-400">If the account exists, reset instructions were requested.</p>:<form className="mt-6 space-y-3" onSubmit={async e=>{e.preventDefault();await api.auth.resetPasswordRequest(email);setDone(true)}}><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email" className="w-full rounded-xl border border-white/10 bg-white/5 p-3 text-white"/><button className="w-full rounded-xl bg-white p-3 text-black">Send reset link</button></form>}</div></main>}